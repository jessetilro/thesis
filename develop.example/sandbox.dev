module test
  model
entity Node {
	
    name : String = concat(this.a.name) <+ ""
    y : Boolean
  }
  relation Edge{
    Node.in *  -> in
    Node.out * -> out
    in.a <-> out.b
  }
  
data

  test : Node {
  	y = true
  }